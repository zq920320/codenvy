/*
 *    Copyright (C) 2013 Codenvy.
 *
 */
package com.codenvy.analytics.server.view;


import org.testng.annotations.Test;

/**
 * @author <a href="mailto:abazko@codenvy.com">Anatoliy Bazko</a>
 */
public class TestTimeLineView {

    @Test
    public void testTimeView() throws Exception {
        // Calendar fromDate = Calendar.getInstance();
        // fromDate.add(Calendar.DAY_OF_MONTH, -1);
        //
        // TimeLineView timeLineView = new TimeLineView(fromDate, TimeUnit.DAY);
        // Assert.assertNotNull(timeLineView);
        //
        // List<Metric> metrics = timeLineView.getMetricList();
        // Assert.assertNotNull(metrics);
        //
        // // int columCount = timeLineView.getMetricList();
        // // Assert.assertTrue(columCount > 0);
        //
        // int rowCount = timeLineView.getRowCount();
        // Assert.assertTrue(rowCount > 0);
        //
        // Iterator<List<String>> rows = timeLineView.getRows();
    }
}
